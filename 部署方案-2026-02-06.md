# 开成不公电商溯源系统 - 部署方案

**部署日期：** 2026-02-06
**目标服务器：** 8.154.32.2
**版本：** v1.2.0（配料回放功能更新）

---

## 📋 项目概况

### 基本信息
- **项目名称：** 开成不公（KCBG）电商溯源系统
- **当前域名：** wx.kcbg.com.cn
- **部署路径：** /www/wwwroot/cwls.turuifanxin.cn/

### 技术栈
- **后端：** PHP 7.3 + FastAdmin + ThinkPHP 5.x
- **前端：** UniApp（跨平台小程序）
- **数据库：** MySQL 5.7.40
- **服务器：** 阿里云 Linux + 宝塔面板

### 服务器信息
- **IP地址：** 8.154.32.2
- **SSH用户：** root
- **SSH密码：** Pp@123456
- **MySQL root密码：** 61d8983da6ee
- **数据库名称：** cwls
- **数据库密码：** ewXHjm77twRm3xL2

---

## 🔄 本次更新内容

### 1. 前端功能变更

#### 视频分类重组
**文件：** `cwlsuniapp/pages/goods/detail.vue`

**变更内容：**
- 视频分类标签从3个增加到4个
- 旧标签：['商品溯源', '精彩瞬间', '多视角回放']
- 新标签：['生产全程回放', '精彩瞬间', '多视角回放', '配料回放']

#### 配料回放交互逻辑
```javascript
// 点击"配料回放"标签（index=3）时，跳转到溯源回放页面
if (index === 3) {
    if (this.info.traceability_url) {
        uni.navigateTo({ url: this.info.traceability_url });
    } else {
        uni.showToast({ title: '暂无配料回放', icon: 'none' });
    }
    return;
}
```

#### 二维码扫码自动定位
- 支持扫码后自动跳转到指定日期和视频类型
- 如果type=3（配料回放），自动跳转到溯源回放页面

#### 提示文案优化
- **旧文案：** "选择商品生产日期，查看该件商品投料全过程溯源"
- **新文案：** "选择产品生产日期，查看该产品生产投料全过程"

### 2. 数据库变更

#### 新增测试数据
**文件：** `add-multi-angle-video-test-data.sql`

为商品ID=1添加3条多视角回放视频（type=2）：
- 俯视角：生产线俯视全景
- 侧视角：关键工序侧面视角
- 特写镜头：精密操作特写

#### 数据结构
```sql
fa_shop_video 表
├── type: SET('0','1','2')  -- 0=生产全程回放, 1=精彩瞬间, 2=多视角回放
├── riqi: DATE              -- 视频日期（2026-02-05）
├── tag: VARCHAR(50)        -- 视频标签
├── shichang: VARCHAR(50)   -- 时长
├── s_time: VARCHAR(50)     -- 开始时间
├── e_time: VARCHAR(50)     -- 结束时间
├── liangdian: VARCHAR(255) -- 亮点描述
└── status: ENUM('0','1')   -- 发布状态（1=已发布）
```

---

## 🚀 部署步骤

### 阶段一：部署前准备（10分钟）

#### 1.1 连接服务器
```bash
ssh root@8.154.32.2
# 输入密码：Pp@123456
```

#### 1.2 检查服务器环境
```bash
# 检查PHP版本（需要7.1+）
php -v

# 检查MySQL版本（需要5.7+）
mysql --version

# 检查Nginx版本
nginx -v

# 检查磁盘空间
df -h
```

#### 1.3 备份数据库
```bash
# 创建备份目录
mkdir -p /backup/$(date +%Y%m%d)

# 备份数据库
mysqldump -uroot -p61d8983da6ee cwls > /backup/$(date +%Y%m%d)/cwls_backup_$(date +%H%M%S).sql

# 验证备份文件
ls -lh /backup/$(date +%Y%m%d)/
```

#### 1.4 备份代码
```bash
# 备份当前代码
cd /www/wwwroot/
tar -czf /backup/$(date +%Y%m%d)/cwls_code_backup_$(date +%H%M%S).tar.gz cwls.turuifanxin.cn/

# 验证备份
ls -lh /backup/$(date +%Y%m%d)/
```

---

### 阶段二：前端部署（15分钟）

#### 2.1 本地构建UniApp
```bash
# 在本地项目目录
cd /Users/peakom/worko/kaichengbugong/cwlsuniapp

# 使用HBuilderX构建微信小程序版本
# 或使用命令行构建
# 输出目录：unpackage/dist/build/mp-weixin/
```

#### 2.2 压缩前端代码
```bash
# 在本地项目根目录
cd /Users/peakom/worko/kaichengbugong

# 只压缩修改的文件
tar -czf cwlsuniapp_deploy.tar.gz cwlsuniapp/pages/goods/detail.vue
```

#### 2.3 上传到服务器
```bash
# 上传压缩包
scp cwlsuniapp_deploy.tar.gz root@8.154.32.2:/tmp/

# 或使用完整前端代码
scp -r cwlsuniapp/ root@8.154.32.2:/tmp/cwlsuniapp_new/
```

#### 2.4 在服务器上部署
```bash
# SSH连接到服务器
ssh root@8.154.32.2

# 备份旧文件
cd /www/wwwroot/cwls.turuifanxin.cn/
cp cwlsuniapp/pages/goods/detail.vue cwlsuniapp/pages/goods/detail.vue.bak

# 解压新文件
cd /www/wwwroot/cwls.turuifanxin.cn/
tar -xzf /tmp/cwlsuniapp_deploy.tar.gz

# 或直接复制
cp -r /tmp/cwlsuniapp_new/* /www/wwwroot/cwls.turuifanxin.cn/cwlsuniapp/
```

---

### 阶段三：数据库更新（5分钟）

#### 3.1 上传SQL文件
```bash
# 在本地执行
scp add-multi-angle-video-test-data.sql root@8.154.32.2:/tmp/
```

#### 3.2 执行SQL脚本
```bash
# SSH连接到服务器
ssh root@8.154.32.2

# 执行SQL脚本
mysql -uroot -p61d8983da6ee cwls < /tmp/add-multi-angle-video-test-data.sql
```

#### 3.3 验证数据
```bash
# 连接数据库
mysql -uroot -p61d8983da6ee cwls

# 执行查询
SELECT id, shop_goods_id, riqi, type, tag, title, status
FROM fa_shop_video
WHERE type = 2
ORDER BY createtime DESC;

# 应该看到3条记录
# 退出
exit;
```

---

### 阶段四：服务重启（5分钟）

#### 4.1 检查配置文件
```bash
# 检查Nginx配置
nginx -t

# 检查PHP-FPM配置
php-fpm -t
```

#### 4.2 重启服务
```bash
# 重启PHP-FPM（PHP 7.3）
systemctl restart php-fpm-73

# 重启Nginx
systemctl restart nginx

# 检查服务状态
systemctl status php-fpm-73
systemctl status nginx
```

#### 4.3 清理缓存
```bash
# 清理PHP缓存
cd /www/wwwroot/cwls.turuifanxin.cn/kcbg/runtime/
rm -rf cache/*
rm -rf log/*
rm -rf temp/*

# 清理Nginx缓存（如果有）
rm -rf /var/cache/nginx/*
```

---

### 阶段五：功能测试（10分钟）

#### 5.1 前端功能测试清单
- [ ] 访问商品详情页（商品ID=1）
- [ ] 验证4个视频分类标签显示正确
  - [ ] 生产全程回放
  - [ ] 精彩瞬间
  - [ ] 多视角回放
  - [ ] 配料回放
- [ ] 点击"配料回放"标签
  - [ ] 如果有traceability_url，跳转到溯源回放页面
  - [ ] 如果没有，显示"暂无配料回放"提示
- [ ] 选择日期"2026-02-05"
- [ ] 点击"多视角回放"标签
  - [ ] 确认显示3个视频
  - [ ] 验证视频标签：俯视角、侧视角、特写镜头
- [ ] 验证提示文案
  - [ ] "选择产品生产日期，查看该产品生产投料全过程"

#### 5.2 二维码功能测试
- [ ] 在后台生成二维码（日期=2026-02-05, type=2）
- [ ] 扫码后确认自动定位到指定日期和视频类型
- [ ] 测试type=3的二维码
  - [ ] 确认跳转到溯源回放页面

#### 5.3 性能测试
- [ ] 页面加载速度正常（<3秒）
- [ ] 视频播放流畅
- [ ] 无JavaScript错误
- [ ] 无网络请求失败

---

## 🔙 回滚方案

### 如果部署出现问题，按以下步骤回滚：

#### 回滚数���库
```bash
# 找到备份文件
ls -lh /backup/$(date +%Y%m%d)/

# 回滚数据库（替换时间戳）
mysql -uroot -p61d8983da6ee cwls < /backup/YYYYMMDD/cwls_backup_HHMMSS.sql
```

#### 回滚代码
```bash
# 恢复备份的文件
cd /www/wwwroot/cwls.turuifanxin.cn/
cp cwlsuniapp/pages/goods/detail.vue.bak cwlsuniapp/pages/goods/detail.vue

# 或完整回滚
cd /www/wwwroot/
rm -rf cwls.turuifanxin.cn/
tar -xzf /backup/YYYYMMDD/cwls_code_backup_HHMMSS.tar.gz
```

#### 重启服务
```bash
systemctl restart php-fpm-73
systemctl restart nginx
```

---

## ⚠️ 风险提示与注意事项

### 高风险项
1. **数据库密码暴露**
   - 建议部署后立即修改MySQL密码
   - 使用强密码策略

2. **测试数据**
   - SQL脚本使用商品ID=1
   - 确认生产环境是否存在该商品
   - 如不存在，需要修改SQL脚本中的shop_goods_id

3. **视频URL**
   - 测试数据使用的是示例视频URL
   - 需要替换为真实的视频地址
   - 确保视频服务器可访问

### 中风险项
1. **前端缓存**
   - 小程序可能有缓存
   - 需要清除缓存或发布新版本
   - 建议用户重新进入小程序

2. **用户体验**
   - 配料回放标签点击后直接跳转
   - 用户可能不理解新的交互方式
   - 建议添加引导提示

3. **兼容性**
   - 确认旧版本用户升级后不会出现问题
   - 测试不同设备和系统版本

### 低风险项
1. **文案变更**
   - 提示文案变更可能需要用户适应
   - 影响较小

2. **日志记录**
   - 建议添加操作日志记录
   - 便于问题追踪

---

## 📝 部署检查清单

### 部署前检查
- [ ] 已备份数据库
- [ ] 已备份代码
- [ ] 已确认服务器环境
- [ ] 已准备回滚方案
- [ ] 已通知相关人员

### 部署中检查
- [ ] 前端代码已上传
- [ ] SQL脚本已执行
- [ ] 数据已验证
- [ ] 服务已重启
- [ ] 配置已验证

### 部署后检查
- [ ] 功能测试通过
- [ ] 性能正常
- [ ] 日志无异常
- [ ] 用户反馈正常
- [ ] 监控告警正常

---

## 🎯 后续建议

### 1. 安全加固
- [ ] 修改数据库密码
- [ ] 启用防火墙规则
- [ ] 配置SSL证书（如未配置）
- [ ] 定期更新系统补丁

### 2. 性能优化
- [ ] 启用Redis缓存
- [ ] 配置CDN加速
- [ ] 优化数据库索引
- [ ] 压缩静态资源

### 3. 监控告警
- [ ] 配置服务器监控
- [ ] 设置错误日志告警
- [ ] 监控数据库性能
- [ ] 配置磁盘空间告警

### 4. 文档更新
- [ ] 更新用户使用手册
- [ ] 更新API文档
- [ ] 更新运维文档
- [ ] 记录本次部署经验

### 5. 灰度发布（可选）
- [ ] 先对10%用户开放
- [ ] 观察1-2天
- [ ] 逐步扩大到50%
- [ ] 最终全量发布

---

## 📞 联系方式

**技术支持：**
- 部署负责人：[填写姓名]
- 联系电话：[填写电话]
- 邮箱：[填写邮箱]

**紧急联系：**
- 如遇紧急问题，立即回滚
- 联系技术负责人
- 记录问题详情

---

## 📅 部署记录

| 日期 | 操作人 | 操作内容 | 结果 | 备注 |
|------|--------|----------|------|------|
| 2026-02-06 | | 部署配料回放功能 | | |
| | | | | |

---

**文档版本：** v1.0
**最后更新：** 2026-02-06
**文档状态：** 待执行
