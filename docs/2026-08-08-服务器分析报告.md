# 服务器分析报告

**日期：** 2026-08-08  
**服务器：** 8.154.32.2  
**用户：** root  

---

## 一、基础环境

| 项目 | 详情 |
|------|------|
| 操作系统 | Alibaba Cloud Linux 3.2104 U10 (OpenAnolis Edition) |
| 内核版本 | 5.10.134-17.2.al8.x86_64 |
| 启动时间 | 2026-02-11 16:07:42 |
| 持续运行 | 25 周 3 天（约 178 天），系统稳定 |
| CPU | 4 核 Intel Xeon Platinum |
| 内存 | 总量 7.3GB，已用 961MB，可用 6.1GB（使用率约 13%） |
| Swap | 1.0GB，当前未使用 |

### 磁盘

| 分区 | 总量 | 已用 | 可用 | 使用率 |
|------|------|------|------|--------|
| /dev/vda3（根分区） | 40G | 12G | 26G | 31% |
| /dev/vda2（EFI） | 200M | 5.8M | 194M | 3% |

> 磁盘空间充裕，短期无压力。

---

## 二、部署状态

### 面板
- **宝塔面板（BT Panel）** 运行中，监听端口 **8888**

### Web 服务 — Nginx

| 虚拟主机 | 协议 | 网站根目录 | 说明 |
|---------|------|-----------|------|
| cwls.turuifanxin.cn | HTTPS | /www/wwwroot/cwls.turuifanxin.cn/public | 主站点 |
| 0.default | HTTP | /www/server/nginx/html | 默认站点 |
| 0.websocket | HTTP | — | WebSocket 代理 |
| phpfpm_status | HTTP | — | PHP-FPM 状态监控 |

### PHP-FPM（多版本共存）

| 版本 | 状态 |
|------|------|
| PHP 7.2.33 | 运行中 |
| PHP 7.4.33 | 运行中 |
| PHP 8.0.26 | 运行中 |
| PHP 8.2.22 | 运行中 |

### 数据库
- **MySQL** 服务运行中，端口 3306（仅本地监听）

### 其他服务
- **Postfix**：邮件服务，运行中
- **Pure-FTPd**：FTP 服务，端口 21，运行中
- **Chronyd**：NTP 时间同步，运行中

---

## 三、网络与安全

### 防火墙开放端口

| 端口 | 用途 |
|------|------|
| 20/tcp, 21/tcp | FTP 数据/控制 |
| 22/tcp | SSH |
| 80/tcp | HTTP |
| 443/tcp | HTTPS |
| 888/tcp | Nginx 辅助端口（宝塔 phpmyadmin） |
| 8888/tcp | 宝塔面板管理 |
| 36449/tcp | 自定义端口 |
| 39000-40000/tcp | FTP 被动模式端口段 |

### 系统日志异常
近期 SSH 日志中出现 `kex_exchange_identification: Connection closed by remote host` 错误，为外部扫描器/爆破机器人连接后被主动断开，属正常现象，无需处理。Nginx 错误日志为空，无报错。

---

## 四、SSL 证书状态

| 配置域名 | 证书 CN | 签发时间 | 到期时间 | 剩余天数 |
|---------|---------|---------|---------|---------|
| cwls.turuifanxin.cn | wx.kcbg.com.cn | 2026-05-17 | **2026-08-15** | **⚠️ 7 天** |

**证书类型：** Let's Encrypt（每期 90 天）  
**自动续签：** **未配置**（crontab 中无续签任务）

---

## 五、风险与待办

### 高优先级

- [ ] **SSL 证书 7 天后过期**，且未配置自动续签。需在 2026-08-15 前续签，否则站点 HTTPS 访问中断。
  - 方案 A：宝塔面板 → SSL 管理 → 手动续签
  - 方案 B：配置 acme.sh 或 certbot cron 实现自动续签

### 中优先级

- [ ] **未配置 SSL 自动续签 cron**，建议补充定时任务避免下次遗漏
- [ ] **PHP 多版本共存（7.2 / 7.4 / 8.0 / 8.2）**，确认各站点实际使用版本，可考虑下线不用的版本减少攻击面
- [ ] **FTP 服务（pure-ftpd）开放**，若无使用需求建议关闭或限制 IP 访问

### 低优先级

- [ ] 宝塔面板端口 8888 建议修改为非默认端口，减少扫描风险
- [ ] 定期检查 MySQL 账号权限，确认无弱密码账号

---

## 六、整体评估

| 维度 | 状态 |
|------|------|
| 系统稳定性 | 良好（178天无重启） |
| 资源使用 | 宽裕（CPU 负载 0.01，内存使用 13%） |
| 磁盘空间 | 健康（31% 使用率） |
| SSL 证书 | 紧急（7天后过期，无自动续签） |
| 安全配置 | 一般（建议加固 FTP、面板端口） |
