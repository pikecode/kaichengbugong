# 微信支付证书更新待办事项

> 创建日期：2026-02-24
> 状态：待处理

## 问题描述

微信支付退款功能报错：**证书验证失败**

**原因**：服务器上的证书文件与配置中的商户号不匹配
- 配置中的商户号：`1695708291`
- 证书文件中的商户号：`1684271848`

## 待办步骤

### 1. 下载正确的证书文件

1. 访问微信商户平台：https://pay.weixin.qq.com
2. 使用商户号 `1695708291` 登录
3. 进入：**账户中心 → API安全 → API证书**
4. 下载 **API证书**（注意：不是 APIv3 密钥）
5. 下载的压缩包中包含：
   - `apiclient_cert.pem` (商户证书)
   - `apiclient_key.pem` (商户私钥)
   - `apiclient_cert.p12` (可选)

**注意事项**：
- 证书必须是商户号 `1695708291` 的
- 下载时可能需要操作员权限或超级管理员权限
- 如果无法下载，请联系商户管理员

### 2. 备份旧证书

```bash
sshpass -p 'Pp@123456' ssh root@8.154.32.2 "cd /www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs && cp apiclient_cert.pem apiclient_cert.pem.bak_$(date +%Y%m%d) && cp apiclient_key.pem apiclient_key.pem.bak_$(date +%Y%m%d)"
```

### 3. 上传新证书到服务器

将下载的证书文件上传到服务器（假设证书在本地 `~/Downloads/cert/` 目录）：

```bash
# 上传商户证书
scp ~/Downloads/cert/apiclient_cert.pem root@8.154.32.2:/www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/

# 上传商户私钥
scp ~/Downloads/cert/apiclient_key.pem root@8.154.32.2:/www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/
```

### 4. 设置证书文件权限

```bash
sshpass -p 'Pp@123456' ssh root@8.154.32.2 "chmod 644 /www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/apiclient_cert.pem && chmod 600 /www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/apiclient_key.pem && chown www:www /www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/apiclient_*.pem"
```

### 5. 验证证书

```bash
# 验证证书商户号是否正确
sshpass -p 'Pp@123456' ssh root@8.154.32.2 "openssl x509 -in /www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/apiclient_cert.pem -noout -subject -enddate"
```

**预期输出**：
- `subject` 中应包含 `CN = 1695708291`
- `notAfter` 显示证书有效期

### 6. 测试退款功能

1. 登录后台：https://wx.kcbg.com.cn/xSZaYJEibq.php
2. 进入：**商城 → 售后管理**
3. 找到待审核的售后申请（如订单号 `20260213061234000000188142`）
4. 审核通过，系统会自动调用微信退款接口
5. 查看是否还报"证书验证失败"错误

## 替代方案

如果无法获取商户号 `1695708291` 的证书，可以考虑：

**修改配置使用现有证书**（如果 `1684271848` 是正确的商户号）：

1. 登录服务器修改配置：
```bash
sshpass -p 'Pp@123456' ssh root@8.154.32.2
cd /www/wwwroot/cwls.turuifanxin.cn
vi addons/epay/config.php
```

2. 找到 `mch_id` 配置项，将 `1695708291` 改为 `1684271848`

3. 清除缓存：
```bash
rm -rf /www/wwwroot/cwls.turuifanxin.cn/runtime/cache/*
rm -rf /www/wwwroot/cwls.turuifanxin.cn/runtime/temp/*
```

4. 同时需要确认微信支付配置中的其他参数（appid、key 等）也对应 `1684271848` 这个商户号

## 相关文件路径

- 证书目录：`/www/wwwroot/cwls.turuifanxin.cn/addons/epay/certs/`
- 支付配置：`/www/wwwroot/cwls.turuifanxin.cn/addons/epay/config.php`
- 退款代码：`/www/wwwroot/cwls.turuifanxin.cn/application/admin/model/shop/Order.php` (refund 方法)

## 完成标志

- [ ] 证书文件已下载
- [ ] 旧证书已备份
- [ ] 新证书已上传
- [ ] 证书权限已设置
- [ ] 证书商户号验证通过
- [ ] 退款功能测试成功
