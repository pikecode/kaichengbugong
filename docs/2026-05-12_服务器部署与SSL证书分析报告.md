# 服务器 8.154.32.2 部署与 SSL 证书分析报告

**检查时间：** 2026-05-12  
**运维人员：** ompeak  
**服务器地址：** 8.154.32.2  
**域名：** wx.kcbg.com.cn / cwls.turuifanxin.cn

---

## 一、服务器概况

| 项目 | 内容 |
|------|------|
| 操作系统 | Linux (CentOS) |
| 运行时长 | 89 天 23 小时 |
| CPU 负载 | 极低 (0.00, 0.01, 0.00) |
| 磁盘使用 | 30% (12G / 40G) |

---

## 二、运行中的服务

### 核心服务状态

| 服务 | 版本 | 状态 | 端口 |
|------|------|------|------|
| Nginx | - | 🟢 运行 | 80, 443 |
| MySQL | 5.7.40 | 🟢 运行 | 3306 |
| PHP-FPM | 7.2 / 7.4 / 8.0 / 8.2 | 🟢 运行 | - |
| SSH | - | 🟢 运行 | 22 |
| OpenLitespeed | - | 🟢 运行 | - |
| Crond | - | 🟢 运行 | - |

### 宝塔面板服务

- 宝塔主服务：`bt.service` 运行中
- 面板管理 Nginx + PHP + MySQL

---

## 三、网站部署状态

### 网站信息

| 项目 | 内容 |
|------|------|
| 网站标题 | 宠物零食 |
| 域名 | wx.kcbg.com.cn |
| 备案域名 | cwls.turuifanxin.cn |
| 网站根目录 | `/www/wwwroot/cwls.turuifanxin.cn/public` |
| HTTP 状态 | 200 ✅ |
| 技术栈 | Bootstrap + PHP |

### 页面结构

- 顶部导航栏（固定定位）
- 品牌名称：宠物零食
- 主要页面：首页、会员中心
- 类型：企业宣传 / 产品展示网站

---

## 四、SSL 证书配置详情

### 证书基本信息

| 项目 | 内容 |
|------|------|
| 颁发机构 | Let's Encrypt (R13) |
| 证书类型 | RSA 2048 位 |
| 证书域名 | wx.kcbg.com.cn |
| 有效期开始 | 2026-02-11 23:04:42 UTC |
| 有效期结束 | **2026-05-12 23:04:41 UTC** |
| 剩余时间 | **15 小时** ⚠️ |

### ⚠️ 紧急警告：证书即将过期

**证书将于 2026-05-12 23:04 UTC 过期，剩余时间不足 16 小时！**

### 证书文件路径

```
/etc/letsencrypt/live/wx.kcbg.com.cn/
├── cert.pem          # 证书文件
├── chain.pem         # 证书链
├── fullchain.pem     # 完整证书链（Nginx 使用）
└── privkey.pem       # 私钥
```

### Nginx SSL 配置

**配置文件：** `/www/server/panel/vhost/nginx/cwls.turuifanxin.cn.conf`

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name wx.kcbg.com.cn;
    
    # SSL 证书配置
    ssl_certificate    /www/server/panel/vhost/cert/cwls.turuifanxin.cn/fullchain.pem;
    ssl_certificate_key    /www/server/panel/vhost/cert/cwls.turuifanxin.cn/privkey.pem;
    
    # SSL 协议与加密套件
    ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers EECDH+CHACHA20:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;
    ssl_prefer_server_ciphers on;
    
    # SSL 会话缓存
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # HSTS
    add_header Strict-Transport-Security "max-age=31536000";
    
    # HTTP 强制跳转 HTTPS
    error_page 497 https://$host$request_uri;
    
    # 网站根目录
    root /www/wwwroot/cwls.turuifanxin.cn/public;
    index index.php index.html;
}
```

### SSL 安全特性

| 特性 | 状态 |
|------|------|
| HTTP/2 | ✅ 已启用 |
| TLS 1.3 | ✅ 已启用 |
| HSTS | ✅ 已启用 (max-age=31536000) |
| HTTP 跳转 HTTPS | ✅ 已配置 |
| 加密套件 | 较强配置 |

### 证书续期配置

**续期方式：** webroot 模式  
**认证文件目录：** `/www/wwwroot/cwls.turuifanxin.cn/public`  
**续期配置：** `/etc/letsencrypt/renewal/wx.kcbg.com.cn.conf`

```ini
authenticator = webroot
webroot_path = /www/wwwroot/cwls.turuifanxin.cn/public
```

---

## 五、备份与归档

网站目录下发现两个备份文件：

| 备份文件 | 大小 | 日期 |
|----------|------|------|
| `cwls.turuifanxin.cn_TiMrr.tar.gz` | 107M | 2025-07-07 |
| `cwls.turuifanxin.cn_W5kya.tar.gz` | 149M | 2024-12-03 |

---

## 六、风险与建议

### 🔴 紧急事项

1. **SSL 证书即将过期**
   - 风险：证书过期后用户将无法访问 HTTPS 网站
   - 行动：立即手动续期或检查自动续期配置

### 🟡 建议优化

1. **版本更新**
   - MySQL 5.7.40 较旧，考虑升级到 8.0
   - PHP 7.2 已停止维护，建议升级到 8.1+

2. **安全加固**
   - SSL 配置中建议禁用 TLS 1.1
   - 确保自动续期任务正常工作

3. **监控系统**
   - 配置证书过期监控告警
   - 设置系统资源监控

---

## 七、常用命令参考

### 证书续期
```bash
# 手动续期证书
certbot renew --cert-name wx.kcbg.com.cn

# 测试续期（干运行）
certbot renew --dry-run

# 强制续期
certbot certonly --force-renew -d wx.kcbg.com.cn
```

### Nginx 管理
```bash
# 重载配置
nginx -s reload

# 测试配置
nginx -t
```

### 服务管理
```bash
# 查看 MySQL 状态
systemctl status mysqld

# 查看 Nginx 状态
systemctl status nginx
```

---

## 八、附录：检查结果原始数据

```
系统运行时间: 15:39:28 up 89 days, 23:31
负载平均: 0.00, 0.01, 0.00

监听端口:
- 80/tcp  nginx
- 443/tcp nginx
- 3306/tcp mysqld
- 22/tcp  sshd

证书信息:
Serial Number: 631917d27bb873fd115680ccff48ab7a5e2
Issuer: C=US, O=Let's Encrypt, CN=R13
```

---

**文档生成时间：** 2026-05-12  
**下次检查建议：** 证书续期后立即验证
